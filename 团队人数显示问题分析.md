# 团队人数显示问题分析

## 问题描述

用户反馈：**"800多个地址的成了200多个了"**

三个用户地址：
- `0x25a5bC4Ecbaf2BF42E1cA89D08e98D800fb939dC`
- `0xdD1A5471d8f8B70500BAE23CC28BC34C7c9ef3b6`
- `0x468F8C39Ef1Db73fe6ecc311B10e1828875273f3`

## 检查结果

### 链上数据查询结果

| 地址 | 新合约 | 旧合约 teamCount | 前端显示 | 问题 |
|------|--------|------------------|----------|------|
| 0x25a5bC4Ecbaf2BF42E1cA89D08e98D800fb939dC | ❌ 不存在 | 414 | 282 | 数据不一致 |
| 0xdD1A5471d8f8B70500BAE23CC28BC34C7c9ef3b6 | ❌ 不存在 | 442 | 279 | 数据不一致 |
| 0x468F8C39Ef1Db73fe6ecc311B10e1828875273f3 | ❌ 不存在 | 126 | 251 | 数据不一致 |

### 问题根源

1. **这三个地址只存在于旧合约，不在新合约中**
2. **前端 `TeamLevel.tsx` 组件只查询新合约** (`protocolContract.userInfo`)
3. **如果用户不在新合约中，前端可能显示错误的数据或0**

## 解决方案

### 方案1: 修改前端同时查询新旧合约（推荐）

修改 `components/TeamLevel.tsx`，使其：
1. 先查询新合约
2. 如果新合约中不存在，再查询旧合约
3. 合并显示数据

### 方案2: 迁移这些用户到新合约

如果这些用户需要继续使用，应该：
1. 将这些用户的数据迁移到新合约
2. 确保 teamCount 等数据正确迁移

### 方案3: 前端显示时提示

如果用户只存在于旧合约，前端应该：
1. 显示提示："该用户数据在旧合约中"
2. 或者显示旧合约的数据

## 代码修改建议

### 修改 `components/TeamLevel.tsx`

```typescript
// 在 fetchTeamInfo 函数中
const fetchTeamInfo = async () => {
  if (isConnected && account && protocolContract) {
    try {
      let userInfo;
      let isFromOldContract = false;
      
      // 先尝试查询新合约
      try {
        userInfo = await protocolContract.userInfo(account);
      } catch (error) {
        // 如果新合约中不存在，查询旧合约
        console.log('用户不在新合约中，查询旧合约...');
        const oldProtocolContract = new ethers.Contract(
          OLD_PROTOCOL_ADDRESS,
          PROTOCOL_ABI,
          provider
        );
        userInfo = await oldProtocolContract.userInfo(account);
        isFromOldContract = true;
      }
      
      // 使用 userInfo 数据...
      const activeDirects = Number(userInfo[1]);
      const teamCount = Number(userInfo[2]);
      
      // 如果来自旧合约，可以显示提示
      if (isFromOldContract) {
        console.warn('⚠️ 用户数据来自旧合约，建议迁移到新合约');
      }
      
      // ... 其余逻辑
    } catch (err) {
      console.error("Failed to fetch team info", err);
    }
  }
}
```

## 验证步骤

1. 修改前端代码
2. 测试这三个地址的显示
3. 验证 teamCount 是否正确显示
4. 检查是否还有其他用户存在同样问题

## 影响范围

- **受影响的用户**: 所有只存在于旧合约的用户
- **影响功能**: 团队人数显示、等级计算、收益计算
- **紧急程度**: 高（用户反馈数据错误）
