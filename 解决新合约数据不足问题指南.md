# 解决新合约数据不足问题指南

## 问题描述

很多用户只存在于旧合约中，不在新合约中，导致：
1. 前端无法正确显示数据
2. 用户无法使用新合约的功能
3. 数据不一致

## 解决方案

### 方案1: 自动发现并迁移所有缺失用户（推荐）

使用新创建的脚本 `scripts/migrate-all-old-contract-users.cjs`，可以：
1. 自动从事件中发现所有用户（新旧合约）
2. 识别只在旧合约中的用户
3. 自动迁移这些用户的所有数据到新合约

#### 使用方法

```bash
# 1. 设置环境变量
export PRIVATE_KEY="你的私钥"
export RPC_URL="https://chain.mcerscan.com/"

# 2. 干运行模式（先预览，不实际执行）
DRY_RUN=true node scripts/migrate-all-old-contract-users.cjs

# 3. 实际执行迁移
node scripts/migrate-all-old-contract-users.cjs
```

#### 迁移内容

脚本会自动迁移以下数据：
- ✅ 推荐关系 (referrer)
- ✅ 活跃直推数 (activeDirects)
- ✅ 团队人数 (teamCount)
- ✅ 总收益 (totalRevenue)
- ✅ 收益上限 (currentCap)
- ✅ 团队总交易量 (teamTotalVolume)
- ✅ 团队总上限 (teamTotalCap)
- ✅ 最大门票金额 (maxTicketAmount, maxSingleTicketAmount)

### 方案2: 手动迁移特定用户

如果只需要迁移特定用户，可以使用 `scripts/migrate-missing-users-complete.cjs`：

```bash
# 1. 编辑脚本，在 MISSING_USERS 数组中添加要迁移的用户地址
# 2. 执行迁移
node scripts/migrate-missing-users-complete.cjs
```

### 方案3: 前端兼容（临时方案）

已经修复的 `TeamLevel.tsx` 组件可以：
- 自动查询旧合约（如果新合约中不存在）
- 正确显示旧合约用户的数据

但这是临时方案，建议还是迁移到新合约。

## 迁移步骤

### 1. 准备工作

```bash
# 确保有足够的 Gas
# 检查 Owner 权限
# 备份当前数据
```

### 2. 执行迁移

```bash
# 使用自动迁移脚本
node scripts/migrate-all-old-contract-users.cjs
```

### 3. 验证迁移结果

```bash
# 检查迁移结果文件
cat output/migration-results-*.json

# 验证特定用户
node scripts/check-user-team-count.cjs
```

## 注意事项

### ⚠️ 重要提示

1. **Gas 费用**: 迁移需要消耗大量 Gas，确保钱包有足够余额
2. **Owner 权限**: 只有合约 Owner 可以执行迁移
3. **数据完整性**: 迁移前建议先备份数据
4. **分批执行**: 如果用户数量很大，建议分批执行

### 迁移顺序

建议按以下顺序迁移：
1. 先迁移推荐关系（确保推荐链完整）
2. 再迁移团队数据（teamCount, activeDirects）
3. 最后迁移财务数据（totalRevenue, currentCap）

### 验证方法

迁移后验证：
```bash
# 1. 检查用户是否在新合约中
node scripts/check-user-team-count.cjs

# 2. 对比新旧合约数据
# 3. 前端测试显示是否正确
```

## 常见问题

### Q: 迁移失败怎么办？

A: 
1. 检查错误信息
2. 确认 Owner 权限
3. 检查 Gas 余额
4. 查看迁移结果文件中的失败记录

### Q: 可以只迁移部分数据吗？

A: 可以，修改脚本中的迁移逻辑，只执行需要的步骤。

### Q: 迁移后旧合约数据会删除吗？

A: 不会，旧合约数据仍然保留，只是在新合约中创建了副本。

### Q: 迁移需要多长时间？

A: 取决于用户数量，每个用户大约需要 1-2 秒（包括 Gas 确认时间）。

## 相关脚本

- `scripts/migrate-all-old-contract-users.cjs` - 自动发现并迁移所有缺失用户
- `scripts/migrate-missing-users-complete.cjs` - 迁移指定用户列表
- `scripts/check-user-team-count.cjs` - 检查用户数据
- `scripts/count-all-contract-users.cjs` - 统计新旧合约用户数

## 后续优化

1. **批量迁移**: 优化脚本支持批量操作，减少 Gas 消耗
2. **断点续传**: 支持中断后继续迁移
3. **进度显示**: 实时显示迁移进度
4. **错误重试**: 自动重试失败的迁移

---

**更新时间**: 2026-01-16  
**状态**: ✅ 可用
