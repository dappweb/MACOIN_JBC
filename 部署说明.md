# 部署说明

## ✅ 构建状态

构建已完成，所有文件已生成在 `dist/` 目录中。

## 📦 本次更新内容

### 1. 修复团队人数显示问题
- ✅ 修复 `TeamLevel.tsx` 支持查询旧合约
- ✅ 添加 `OLD_PROTOCOL` 地址到 `Web3Context.tsx`
- ✅ 如果用户不在新合约中，自动从旧合约查询数据

### 2. 数据同步完成
- ✅ 同步了 4 个用户的数据从旧合约到新合约
- ✅ 团队人数、总收益、团队总交易量等字段已更新

## 🚀 部署方式

### 方式1: 使用 Cloudflare Pages（推荐）

```bash
# 如果已配置 wrangler 登录
npx wrangler pages deploy dist --project-name=jbc-ac-production
```

### 方式2: 使用 GitHub Actions

如果配置了 GitHub Actions，推送代码到主分支会自动触发部署。

```bash
git add .
git commit -m "修复团队人数显示问题，同步旧合约数据"
git push origin main
```

### 方式3: 手动上传到 Cloudflare Dashboard

1. 登录 Cloudflare Dashboard
2. 进入 Pages 项目 `jbc-ac-production`
3. 上传 `dist/` 目录中的文件

## 📋 部署前检查清单

- [x] 代码构建成功
- [x] 无 lint 错误
- [x] 数据同步完成
- [ ] 环境变量已配置
- [ ] Cloudflare 认证已设置

## 🔍 验证部署

部署后验证：
1. 访问生产环境网站
2. 使用以下地址测试团队人数显示：
   - `0x25a5bC4Ecbaf2BF42E1cA89D08e98D800fb939dC` (应该显示 414)
   - `0xdD1A5471d8f8B70500BAE23CC28BC34C7c9ef3b6` (应该显示 442)
   - `0x468F8C39Ef1Db73fe6ecc311B10e1828875273f3` (应该显示 126)
   - `0x5B7E080Daa6A6437B50eb881d84b66158Bb10fE5` (应该显示 549)

## 📝 注意事项

1. **wrangler 需要交互式环境**：如果使用命令行部署，需要先登录
   ```bash
   npx wrangler login
   ```

2. **环境变量**：确保生产环境的环境变量已正确配置

3. **缓存清理**：部署后可能需要清理浏览器缓存才能看到更新

---

**构建时间**: 2026-01-16 18:28:31  
**构建状态**: ✅ 成功
