# 直接部署到 Cloudflare Pages

## ✅ 可以直接部署！

使用 `wrangler pages deploy` 命令可以直接部署，**不需要**先在 Cloudflare Dashboard 中创建项目。

### 🚀 直接部署步骤

#### 方法 1: 使用 npm 脚本（推荐）

```bash
# 构建并部署到生产环境
npm run pages:deploy:prod
```

这个命令会：
1. 自动构建前端 (`npm run build`)
2. 使用 wrangler 部署到 Cloudflare Pages
3. 如果项目不存在，wrangler 会自动创建

#### 方法 2: 手动部署

```bash
# 1. 先构建
npm run build

# 2. 登录 Cloudflare（首次使用需要）
npx wrangler login

# 3. 直接部署
npx wrangler pages deploy dist --project-name=jbc-ac-production
```

### 📋 前置条件

在直接部署之前，确保：

1. **已配置 Cloudflare 凭证**
   - 环境变量 `CLOUDFLARE_API_TOKEN` 和 `CLOUDFLARE_ACCOUNT_ID`
   - 或已通过 `wrangler login` 登录

2. **已构建前端**
   - `dist/` 目录存在且包含构建产物

3. **API Token 权限**
   - Cloudflare Pages → Edit
   - Account Settings → Read

### 🔧 使用环境变量

如果使用环境变量而不是 `wrangler login`：

```bash
export CLOUDFLARE_API_TOKEN="your-api-token"
export CLOUDFLARE_ACCOUNT_ID="your-account-id"

npm run pages:deploy:prod
```

### ⚠️ 注意事项

1. **项目名称**: 如果项目不存在，wrangler 会自动创建。确保项目名称唯一。
2. **首次部署**: 首次部署可能需要几分钟来创建项目。
3. **环境变量**: 部署后需要单独设置环境变量（使用 `wrangler pages secret put`）

### 📝 设置环境变量（部署后）

部署完成后，设置环境变量：

```bash
# 设置环境变量
npx wrangler pages secret put JBC_CONTRACT_ADDRESS \
  --text "0x..." \
  --project-name=jbc-ac-production

npx wrangler pages secret put PROTOCOL_CONTRACT_ADDRESS \
  --text "0x..." \
  --project-name=jbc-ac-production

# ... 其他环境变量
```

### ✅ 验证部署

部署成功后，访问：
- `https://jbc-ac-production.pages.dev`
- 或你配置的自定义域名

---

**总结**: 可以直接部署！只需要配置好 Cloudflare 凭证，然后运行 `npm run pages:deploy:prod` 即可。
