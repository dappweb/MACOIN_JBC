# 修复部署连接错误

## ❌ 错误信息
```
upstream connect error or disconnect/reset before headers. 
reset reason: connection termination
```

## 🔍 可能的原因

1. **网络连接不稳定**
   - 本地网络到 Cloudflare 的连接中断
   - 防火墙或代理问题

2. **部署文件过大**
   - `dist/` 目录文件太大
   - 上传超时

3. **Cloudflare 服务端问题**
   - 临时服务中断
   - 限流保护

4. **部署超时**
   - 部署过程太长导致连接超时

## ✅ 解决方案

### 方案 1: 重试部署（最简单）

```bash
# 直接重试
npm run pages:deploy:prod
```

### 方案 2: 分步部署（更稳定）

```bash
# 1. 先构建
npm run build

# 2. 检查构建产物大小
du -sh dist/

# 3. 如果文件太大，考虑优化
# 然后单独部署
npx wrangler pages deploy dist --project-name=jbc-ac-production
```

### 方案 3: 使用 GitHub Actions 部署（推荐）

GitHub Actions 有更好的网络环境和重试机制：

1. 推送到 `p-prod` 分支
2. GitHub Actions 会自动部署
3. 有自动重试机制

```bash
git add .
git commit -m "触发部署"
git push origin p-prod
```

### 方案 4: 检查并优化构建产物

```bash
# 检查 dist 目录大小
du -sh dist/

# 如果太大（>50MB），考虑：
# 1. 检查是否有不必要的文件
# 2. 优化图片资源
# 3. 启用压缩
```

### 方案 5: 使用环境变量设置超时

```bash
# 增加超时时间
export WRANGLER_TIMEOUT=300  # 5分钟
npm run pages:deploy:prod
```

## 🔧 诊断步骤

1. **检查网络连接**
   ```bash
   ping api.cloudflare.com
   ```

2. **检查 wrangler 版本**
   ```bash
   npx wrangler --version
   ```

3. **检查登录状态**
   ```bash
   npx wrangler whoami
   ```

4. **检查构建产物**
   ```bash
   ls -lh dist/
   du -sh dist/
   ```

## 💡 最佳实践

1. **使用 GitHub Actions 部署**（最稳定）
   - 自动重试
   - 更好的网络环境
   - 完整的日志记录

2. **如果必须本地部署**
   - 确保网络稳定
   - 使用 VPN 或代理（如果需要）
   - 分步执行（先构建，再部署）

3. **优化构建产物**
   - 压缩资源
   - 移除不必要的文件
   - 使用 CDN 加载大型资源

## 🚀 推荐操作

**立即执行**：使用 GitHub Actions 部署（最可靠）

```bash
git add .
git commit -m "触发 Cloudflare Pages 部署"
git push origin p-prod
```

然后查看部署状态：
https://github.com/dappweb/MACOIN_JBC/actions
