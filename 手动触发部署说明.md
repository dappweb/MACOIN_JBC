# 手动触发 Cloudflare Pages 部署

## 方式1: 通过 GitHub Actions 手动触发（推荐）

### 步骤：

1. 访问 GitHub Actions 页面：
   ```
   https://github.com/dappweb/MACOIN_JBC/actions/workflows/deploy-production.yml
   ```

2. 点击右侧的 "Run workflow" 按钮

3. 选择分支：`p-prod`

4. 勾选 "强制部署到生产环境 (jbc.ac)"（如果需要跳过测试）

5. 点击 "Run workflow" 开始部署

## 方式2: 使用 GitHub CLI（如果已安装）

```bash
gh workflow run deploy-production.yml \
  --ref p-prod \
  -f force_deploy=true
```

## 方式3: 使用 Cloudflare Dashboard

1. 登录 Cloudflare Dashboard
2. 进入 Workers & Pages
3. 选择项目 `jbc-ac-production`
4. 点击 "Upload assets"
5. 上传 `dist/` 目录中的所有文件

## 方式4: 使用 Wrangler（需要交互式环境）

如果可以在交互式终端中运行：

```bash
# 1. 确保已登录
npx wrangler login

# 2. 部署
npx wrangler pages deploy dist --project-name=jbc-ac-production
```

## 当前状态

- ✅ 代码已构建完成（`dist/` 目录）
- ✅ 代码已推送到 GitHub (`p-prod` 分支)
- ❌ GitHub Actions 部署失败（需要检查日志）

## 建议

**最快的方式**：通过 GitHub Actions 页面手动触发部署，并勾选"强制部署"选项跳过测试。

---

**查看部署日志**：
https://github.com/dappweb/MACOIN_JBC/actions/runs/21065026096
