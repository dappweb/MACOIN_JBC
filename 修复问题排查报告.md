# 用户无法赎回/提供流动性问题修复报告

## 📅 日期
2026-02-11

## 🔍 问题分析

用户反馈无法赎回或无法提供流动性，可能原因包括：
1. 门票过期（72小时规则）
2. 合约暂停
3. 功能禁用（质押/赎回）
4. 达到收益上限
5. 池子不足
6. 余额不足
7. 推荐人未绑定

---

## ✅ 修复内容

### 一、MiningPanel.tsx 修复

#### 1. 批量赎回手续费计算修复

**问题**：只计算了一笔质押的手续费，导致多笔质押同时到期时交易 revert

**修复**：遍历所有质押，统计到期且需要付费的质押数量
```typescript
// 正确计算：N 笔新逻辑质押 × feeBase × 1%
for (const stake of stakes) {
    maturedCount++;
    let feePaid = 0n;
    if (typeof protocolContract.stakeRedemptionFeePaid === 'function') {
        feePaid = await protocolContract.stakeRedemptionFeePaid(stake.id);
    }
    if (feePaid === 0n) {
        newLogicCount++;
        expectedFee += singleFee;
    }
}
```

#### 2. 合约状态检查（新增）

**新增状态**：
```typescript
const [contractStatus, setContractStatus] = useState({
  paused: false,
  liquidityEnabled: true,
  redeemEnabled: true
});
```

**质押前检查**：
```typescript
if (contractStatus.paused) {
    toast.error("合约已紧急暂停，暂时无法操作");
    return;
}
if (!contractStatus.liquidityEnabled) {
    toast.error("质押功能已暂停，请联系管理员");
    return;
}
```

**赎回前检查**：
```typescript
if (contractStatus.paused) {
    toast.error("合约已紧急暂停，暂时无法赎回");
    return;
}
if (!contractStatus.redeemEnabled) {
    toast.error("赎回功能已暂停，请联系管理员");
    return;
}
```

#### 3. 收益上限检查（新增）

```typescript
if (ticketInfo && ticketInfo.currentCap > 0n && ticketInfo.totalRevenue >= ticketInfo.currentCap) {
    toast.error("已达到3倍收益上限，请重新购买门票");
    return;
}
```

#### 4. 门票过期检查（新增）

```typescript
if (isSubjectToExpiry) {
    const deadline = deadlineBase + ticketFlexibilityDuration;
    
    if (now > deadline) {
        toast.error("门票已过期（72小时未质押），请重新购买门票");
        return;
    } else if (remaining < 3600) {
        toast.warning(`门票即将过期，剩余时间：X 分钟`);
    }
}
```

---

### 二、LiquidityPositions.tsx 修复

#### 1. 合约状态检查（新增）

```typescript
const [contractStatus, setContractStatus] = useState({
  paused: false,
  redeemEnabled: true
});
```

**单笔赎回前检查**：
```typescript
if (contractStatus.paused) {
  toast.error("合约已紧急暂停，暂时无法赎回");
  return;
}
if (!contractStatus.redeemEnabled) {
  toast.error("赎回功能已暂停，请联系管理员");
  return;
}
```

#### 2. 错误提示增强

新增错误类型识别：
- `unauthorized` → "无权操作该质押，请确认质押归属"
- `insufficientbalance` → "合约余额不足，请联系管理员"
- `actiontooearly` → "质押尚未到期，请等待到期后再试"

---

### 三、诊断脚本（新增）

**文件**：`scripts/diagnose-user-issue.cjs`

**功能**：
1. 检查合约状态（暂停、功能开关）
2. 检查用户信息（推荐人、收益、上限）
3. 检查门票状态（过期、退出）
4. 检查质押列表（到期状态、手续费）
5. 检查池子余额
6. 检查用户余额
7. 检查收益上限

**用法**：
```bash
node scripts/diagnose-user-issue.cjs 0x用户地址
```

**输出示例**：
```
🔍 诊断用户: 0x1234...

📋 检查合约状态...
  ✅ 合约未暂停
  ✅ 质押功能已启用
  ✅ 赎回功能已启用

📋 检查门票状态...
  门票金额: 1000 MC
  ❌ 门票已过期（72小时未质押）

📊 诊断结果汇总
🔴 阻止操作的问题:
  ❌ 门票已过期（72小时未质押）

💡 建议操作:
  2. 购买新门票
```

---

## 📋 修复验证清单

| 功能 | 状态 | 说明 |
|------|------|------|
| 批量赎回手续费计算 | ✅ 已修复 | 正确计算 N 笔质押的手续费 |
| 合约暂停检查 | ✅ 新增 | 质押/赎回前检查 |
| 功能开关检查 | ✅ 新增 | liquidityEnabled / redeemEnabled |
| 收益上限检查 | ✅ 新增 | 达到3倍上限时提示 |
| 门票过期检查 | ✅ 新增 | 72小时规则验证 |
| 单笔赎回逻辑 | ✅ 已验证 | LiquidityPositions.tsx 逻辑正确 |
| 错误提示增强 | ✅ 已优化 | 更详细的错误类型识别 |
| 诊断脚本 | ✅ 新增 | 帮助排查用户问题 |

---

## 🚀 部署建议

### 测试场景

1. **合约暂停场景**
   - 管理员暂停合约后，用户尝试质押/赎回
   - 验证提示信息："合约已紧急暂停..."

2. **功能禁用场景**
   - 管理员禁用质押/赎回功能
   - 验证提示信息："质押/赎回功能已暂停..."

3. **门票过期场景**
   - 72小时后尝试复投
   - 验证提示信息："门票已过期（72小时未质押）..."

4. **收益上限场景**
   - 达到3倍上限后尝试质押
   - 验证提示信息："已达到3倍收益上限..."

5. **批量赎回场景**
   - 3笔以上质押同时到期
   - 验证手续费计算正确

### 监控指标

- 用户操作失败率
- 各类错误提示出现频率
- 诊断脚本使用情况和结果

---

## 📝 用户沟通建议

发布公告说明：
1. **功能增强**：新增更多操作前的状态检查，避免无效交易
2. **错误提示优化**：更明确地告知无法操作的原因
3. **72小时规则提醒**：强调门票有效期，建议及时质押
4. **诊断工具**：提供自助排查工具给客服和高级用户

---

## 🔄 后续优化建议

1. **前端状态显示**：在界面上显示合约状态（暂停/功能开关）
2. **收益上限预警**：在达到80%、90%时显示预警提示
3. **门票过期倒计时**：更明显的倒计时提醒
4. **批量操作确认**：批量赎回前显示手续费预估确认弹窗
