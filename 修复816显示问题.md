# 🔧 修复线上仅显示816的问题

## 问题描述

线上显示的用户总数是 **816**，但实际应该是 **945**（新旧合约合并后的唯一账户总数）。

## 问题原因

`AdminFinancialDataUpdater.tsx` 组件中的 `scanUserFinancialData` 函数只从**新合约**查询事件，没有查询**旧合约**的事件，导致统计的用户数不完整。

### 实际数据
- **新合约账户数**: 652
- **旧合约账户数**: 377
- **合并后唯一账户总数**: 945
- **之前显示**: 816（不完整）

## 修复内容

### 修改文件
`components/AdminFinancialDataUpdater.tsx`

### 修改内容

1. **添加旧合约地址常量**
   ```typescript
   const OLD_PROTOCOL_ADDRESS = "0x77601aC473dB1195A1A9c82229C9bD008a69987A";
   ```

2. **导入 PROTOCOL_ABI**
   ```typescript
   import { useWeb3, PROTOCOL_ABI } from '../src/Web3Context';
   ```

3. **修改扫描函数，同时查询新旧合约**
   - 创建旧合约实例
   - 分别查询新旧合约的事件
   - 合并所有事件
   - 去重统计用户总数

### 关键修改

```typescript
// 创建旧合约实例
const oldProtocolContract = new ethers.Contract(OLD_PROTOCOL_ADDRESS, PROTOCOL_ABI, provider);

// 获取新合约的所有相关事件
const [newTicketEvents, newStakeEvents, newRewardEvents, newRedeemEvents] = await Promise.all([...]);

// 获取旧合约的所有相关事件
const [oldTicketEvents, oldStakeEvents, oldRewardEvents, oldRedeemEvents] = await Promise.all([...]);

// 合并新旧合约的事件
const ticketEvents = [...newTicketEvents, ...oldTicketEvents];
const stakeEvents = [...newStakeEvents, ...oldStakeEvents];
const rewardEvents = [...newRewardEvents, ...oldRewardEvents];
const redeemEvents = [...newRedeemEvents, ...oldRedeemEvents];
```

## 修复效果

修复后，`AdminFinancialDataUpdater` 组件会：
- ✅ 同时查询新旧合约的所有事件
- ✅ 正确统计所有用户（包括新旧合约）
- ✅ 显示正确的总用户数：**945**（而不是816）

## 验证方法

1. **运行统计脚本验证**
   ```bash
   node scripts/count-all-contract-users.cjs
   ```
   应该显示：
   - 新合约账户总数: 652
   - 旧合约账户总数: 377
   - 合并后唯一账户总数: 945

2. **在管理面板中验证**
   - 访问管理面板
   - 进入"用户财务数据批量更新"
   - 点击"扫描链上事件"
   - 查看"总用户数"，应该显示 **945**

## 部署说明

修复完成后需要：
1. 重新构建前端
   ```bash
   npm run build
   ```

2. 部署到生产环境
   ```bash
   npm run pages:deploy:prod
   ```

## 相关文件

- `components/AdminFinancialDataUpdater.tsx` - 修复的文件
- `scripts/count-all-contract-users.cjs` - 统计脚本（用于验证）
- `账户统计报告.md` - 详细的统计报告

---

**修复时间**: 2026-01-16  
**修复状态**: ✅ 已完成
