# 团队人数显示问题修复报告

## 问题描述

用户反馈：**"800多个地址的成了200多个了"**

三个用户地址：
- `0x25a5bC4Ecbaf2BF42E1cA89D08e98D800fb939dC`
- `0xdD1A5471d8f8B70500BAE23CC28BC34C7c9ef3b6`
- `0x468F8C39Ef1Db73fe6ecc311B10e1828875273f3`

## 问题根源

1. **这三个地址只存在于旧合约，不在新合约中**
2. **前端 `TeamLevel.tsx` 组件只查询新合约** (`protocolContract.userInfo`)
3. **如果用户不在新合约中，查询会失败，导致显示错误或0**

### 链上数据查询结果

| 地址 | 新合约 | 旧合约 teamCount | 前端显示 | 问题 |
|------|--------|------------------|----------|------|
| 0x25a5bC4Ecbaf2BF42E1cA89D08e98D800fb939dC | ❌ 不存在 | 414 | 282 | 数据不一致 |
| 0xdD1A5471d8f8B70500BAE23CC28BC34C7c9ef3b6 | ❌ 不存在 | 442 | 279 | 数据不一致 |
| 0x468F8C39Ef1Db73fe6ecc311B10e1828875273f3 | ❌ 不存在 | 126 | 251 | 数据不一致 |

## 修复方案

### 1. 添加旧合约地址到 `Web3Context.tsx`

```typescript
export const CONTRACT_ADDRESSES = {
  // ... 其他地址
  OLD_PROTOCOL: "0x77601aC473dB1195A1A9c82229C9bD008a69987A", // 旧协议合约地址
};
```

### 2. 修改 `TeamLevel.tsx` 支持查询旧合约

**修改内容**：
1. 导入 `CONTRACT_ADDRESSES` 和 `PROTOCOL_ABI`
2. 从 `useWeb3()` 获取 `provider`
3. 在 `fetchTeamInfo` 函数中：
   - 先尝试查询新合约
   - 如果新合约中不存在，再查询旧合约
   - 如果用户来自旧合约，跳过直推列表查询（旧合约可能不支持）

**关键代码**：

```typescript
// 先尝试查询新合约
try {
  userInfo = await protocolContract.userInfo(account);
} catch (error) {
  // 如果新合约中不存在，尝试查询旧合约
  if (provider && CONTRACT_ADDRESSES.OLD_PROTOCOL) {
    const oldProtocolContract = new ethers.Contract(
      CONTRACT_ADDRESSES.OLD_PROTOCOL,
      PROTOCOL_ABI,
      provider
    );
    userInfo = await oldProtocolContract.userInfo(account);
    isFromOldContract = true;
  }
}

// 如果用户来自旧合约，跳过直推列表查询
if (!isFromOldContract) {
  // 查询直推列表...
} else {
  setDirectReferrals([]);
}
```

## 修复效果

修复后：
- ✅ 如果用户在新合约中，正常显示新合约数据
- ✅ 如果用户不在新合约中，自动查询旧合约并显示旧合约数据
- ✅ 团队人数、等级等信息都能正确显示
- ✅ 避免显示错误或0的情况

## 修改的文件

1. `src/Web3Context.tsx` - 添加 `OLD_PROTOCOL` 地址
2. `components/TeamLevel.tsx` - 支持查询旧合约

## 验证步骤

1. 使用这三个地址连接钱包
2. 访问团队节点页面
3. 验证团队人数是否正确显示（应该显示旧合约中的值）
4. 验证等级计算是否正确

## 注意事项

1. **旧合约用户建议迁移**：虽然现在可以显示旧合约数据，但建议这些用户迁移到新合约
2. **直推列表**：旧合约用户可能无法显示直推列表（如果旧合约不支持 `getDirectReferrals`）
3. **功能限制**：旧合约用户可能无法使用新合约的功能（购买门票、质押等）

## 后续建议

1. **数据迁移**：考虑将这些用户的数据迁移到新合约
2. **用户通知**：通知旧合约用户迁移到新合约
3. **统一数据源**：长期目标是所有用户都在新合约中

---

**修复时间**: 2026-01-16  
**修复状态**: ✅ 已完成
